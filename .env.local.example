# Copy this to .env.local and fill in. Never commit .env.local.

# --- Sanity (Project ID 80kiihr6 confirmed) ---
NEXT_PUBLIC_SANITY_PROJECT_ID=80kiihr6
NEXT_PUBLIC_SANITY_DATASET=production
# Only needed for write operations (the ingestion script). Create via sanity.io/manage → API → Tokens.
SANITY_API_WRITE_TOKEN=

# --- Analytics (add when ready) ---
NEXT_PUBLIC_GA_MEASUREMENT_ID=
